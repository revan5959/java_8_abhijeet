package test;

import java.util.Comparator;

public class HelperCLasss  implements Comparator<HelperCLasss>{

	public int compare(SortByComprartorObject o1, SortByComprartorObject o2) {
		return o1.getId().compareTo(o2.getId());
	}
}
